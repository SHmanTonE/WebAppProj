@model IList<IGrouping<string, MasterProj.Models.Store>>

@{
    ViewBag.Title = "groupbyresult";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Group By Store City</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Store City:
        </th>
        <th>
            Number of Stores:
        </th>

    </tr>

    @for (int i = 0; i < Model.Count; i++)
    {
        <tr>
            <td>
                @Model[i].Key
            </td>
            <td>
                @Model[i].Count()
            </td>

        </tr>
    }

</table>
